# ---- Core (required to run the service with the mock provider) ----
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
httpx==0.27.2

# ---- Optional integrations (lazily imported; install to enable) ----
# Real LLM provider (set LLM_PROVIDER=anthropic):
# anthropic==0.34.2
# Agent orchestration (LangGraph). The service falls back to a sequential
# runner if langgraph is not installed:
# langgraph==0.2.28
# Vector store (set QDRANT_URL=...). Falls back to an in-memory store otherwise:
# qdrant-client==1.11.2
# Document pipeline (OCR / PDF / DOCX):
# pypdf==4.3.1
# python-docx==1.1.2
# pytesseract==0.3.13
# pillow==10.4.0
